# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/

# Tooling
.uv/

# Secrets and machine-specific dev settings — never commit
.env
*.password

# Dev sandbox configuration (copy of the user config): the duplicacy
# cache is regenerable and bulky, so only settings are committed
sandbox-config/repo/.duplicacy/cache/

# IDE
.idea/